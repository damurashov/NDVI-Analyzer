function [ mean_ndvi, green_perc ] = get_vec_meanndvi(f_use_mask);

global g_images
nimgs = length(g_images.files_nir);

for i = 1 : nimgs
	ndvi_map = get_ndvi_map(f_use_mask);
	
	for i = 1 : size(ndvi_map,1)
		for j = 1 : size(ndvi_map,2)
			
		end
	end
end

end

